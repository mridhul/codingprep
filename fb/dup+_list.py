def duplicate_items(list_numbers):
    ret_list=[]
    list_numbers.sort()
    for i in xrange(len(list_numbers) -1):
        if list_numbers[i]==list_numbers[i+1]
        ret_list.append(i)
    return[ret_list]